ScriptGuard: Advanced Deobfuscation & Malware Analysis Framework



ScriptGuard — это мощный автономный инструмент для статического анализа вредоносных и обфусцированных скриптов. Он создан для специалистов по кибербезопасности, аналитиков SOC и исследователей вредоносного ПО, чтобы автоматизировать процесс "распутывания" многослойного кода, который часто встречается в фишинговых атаках и вредоносных вложениях.

🎯 Проблема
Современные атаки все чаще полагаются на обфусцированные скрипты (PowerShell, JavaScript, VBS) и макросы (VBA), встроенные в документы. Ручной анализ таких угроз — это трудоемкий и сложный процесс, требующий глубоких знаний и времени. ScriptGuard автоматизирует эту работу, позволяя аналитикам быстро получать ключевую информацию об угрозе.

✨ Ключевые возможности
Многоэтапная деобфускация: Автоматически и итеративно применяет различные техники декодирования (Base64, Hex, URL-decode) и преобразования строк для раскрытия исходного кода.

Анализ различных форматов: Поддерживает .js, .vbs, .ps1, .html, а также извлечение макросов из OLE-контейнеров (документы Microsoft Office .docm, .xlsm и т.д.).

Извлечение индикаторов компрометации (IOC): Автоматически находит и валидирует IP-адреса, домены, URL-адреса и хеши файлов.

Сопоставление с MITRE ATT&CK®: Связывает обнаруженные техники и тактики с общепринятой базой знаний, предоставляя контекст для атаки.

Структурированные отчеты: Генерирует подробный JSON-отчет, готовый для интеграции с другими системами (SIEM, TIP).

Полностью автономная работа: Не требует доступа в интернет, что обеспечивает безопасность при анализе высокорискованных образцов.

Расширяемая архитектура: Легко добавлять новые загрузчики, модули деобфускации (трансформеры) и экстракторы IOC.

⚙️ Как это работает: Архитектура конвейера (Pipeline)
В основе ScriptGuard лежит гибкий конвейер (pipeline), который обрабатывает данные по шагам. Основная единица данных — FileFragment, представляющий собой кусок кода или данных.

ЗАГРУЗКА (Loaders): Loader читает исходный файл (например, .docm или .js) и извлекает из него начальные фрагменты. Например, OleLoader извлечет VBA-макросы, а HtmlLoader — содержимое тегов <script>.

ПРЕОБРАЗОВАНИЕ (Transforms): Engine (движок) передает каждый фрагмент через набор Transforms. Трансформер — это модуль, выполняющий одну операцию деобфускации (например, декодирование из Base64). Если трансформация успешна, он создает новый, "очищенный" FileFragment. Движок продолжает этот процесс итеративно, пока ни один из трансформеров не сможет примениться к существующим фрагментам.

ИЗВЛЕЧЕНИЕ (Extractors): После завершения деобфускации Extractor'ы сканируют все фрагменты (исходные и полученные) на предмет индикаторов компрометации (IOC).

ОТЧЕТ (Report): Модуль Report собирает все найденные IOC, обнаруженные техники, строит дерево преобразований (как изначальный код превратился в конечный) и формирует итоговый JSON-отчет.

🚀 Начало работы
Установка
Для работы ScriptGuard требуется Python 3.9 или выше.

# 1. Клонируйте репозиторий
git clone https://github.com/ваш-аккаунт/scriptguard.git
cd scriptguard

# 2. Установите зависимости (необходимо создать файл requirements.txt)
pip install -r requirements.txt
Файл requirements.txt должен содержать:

oletools
python-magic
charde t
beautifulsoup4
Использование
Основной интерфейс для работы с инструментом — cli.py.

# Просто проанализировать файл и вывести результат в консоль
python cli.py /path/to/suspicious_file.js

# Проанализировать документ с макросом и сохранить отчет в файл
python cli.py document.docm --output report.json
🏗️ Структура проекта
Проект имеет модульную структуру для легкой поддержки и расширения.

scriptguard/
 ├── core/
 │   ├── model/         # Классы данных (FileFragment, IOCs)
 │   ├── loader/        # Загрузчики файлов (Text, OLE, HTML)
 │   ├── transforms/    # Модули деобфускации (Base64, Hex, и т.д.)
 │   ├── extractors/    # Извлечение IOC (IP, URL)
 │   ├── engine/        # Ядро конвейера (планировщик, управление состоянием)
 │   ├── ioc/           # Логика валидации и представления IOC
 │   ├── mitre/         # Логика для сопоставления с MITRE ATT&CK
 │   ├── report/        # Генерация JSON-отчетов
 │   └── pipeline.py    # Главный оркестратор, собирающий все вместе
 ├── cli.py             # Интерфейс командной строки
 └── README.md
🤝 Участие в разработке (Contribution)
Мы приветствуем любой вклад в развитие проекта! Если вы хотите добавить новый функционал, пожалуйста, следуйте установленной архитектуре:

Новый загрузчик: Унаследуйте свой класс от core.loader.base.BaseLoader и реализуйте метод load.

Новый метод деобфускации: Создайте новый класс в core/transforms/, который принимает FileFragment и возвращает новый, если трансформация возможна.

Новый экстрактор IOC: Добавьте логику в модуль core/extractors/.

Перед созданием Pull Request убедитесь, что ваш код отформатирован и (в идеале) покрыт тестами.

🗺️ Дорожная карта (Roadmap)
У нас большие планы по развитию ScriptGuard:


Расширение поддержки техник: Добавление декодеров для специфичных техник обфускации (например, XOR с переменным ключом, строковые операции VBS).


Динамический анализ: Интеграция с песочницами для выполнения "безопасного" кода и анализа его поведения.


Улучшенный анализ форматов: Поддержка PDF-файлов, Android APK и других контейнеров.


Интеграция с Threat Intelligence: Возможность проверки найденных IOC по базам VirusTotal, AbuseIPDB и др.


Визуализация: Создание графического веб-интерфейса для визуализации дерева деобфускации и отчетов.

📜 Лицензия
Проект распространяется под лицензией MIT. Подробности см. в файле LICENSE.
